# Production Secrets (sensitive - DO NOT COMMIT)
# Copy this to .env.secrets.prod and add your actual production keys

# Django Secret Key (generate a new, strong key for production)
SECRET_KEY=your-production-secret-key-here-make-it-very-long-and-random

# API Keys (production keys)
GOOGLE_MAPS_API_KEY=your-production-google-maps-api-key
OPENAI_API_KEY=your-production-openai-api-key

# Database credentials
DATABASE_PASSWORD=your-production-database-password
DATABASE_URL=postgresql://user:password@host:port/dbname

# Email credentials (production email service)
EMAIL_HOST_USER=your-production-email@yourdomain.com
EMAIL_HOST_PASSWORD=your-production-app-password

# Third-party service keys (production keys)
STRIPE_SECRET_KEY=sk_live_your_stripe_live_key
STRIPE_PUBLISHABLE_KEY=pk_live_your_stripe_live_key

# Social auth keys (production OAuth apps)
GOOGLE_CLIENT_ID=your-production-google-client-id
GOOGLE_CLIENT_SECRET=your-production-google-client-secret

# Analytics keys (production properties)
GOOGLE_ANALYTICS_ID=your-production-analytics-id

# Storage credentials (production buckets)
AWS_ACCESS_KEY_ID=your-production-aws-access-key
AWS_SECRET_ACCESS_KEY=your-production-aws-secret-key
AWS_STORAGE_BUCKET_NAME=your-production-bucket

# Redis credentials (production Redis instance)
REDIS_URL=redis://production-redis-host:6379/0

# Monitoring/logging keys (production)
SENTRY_DSN=your-production-sentry-dsn

# Cloud SQL connection (if using Cloud SQL)
CLOUD_SQL_CONNECTION_NAME=your-project:region:instance